# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

version: 1
filesystem_policy:
  include_workdir: true
  read_only:
  - /usr
  - /lib
  - /opt/hermes
  - /proc
  - /dev/urandom
  - /app
  - /etc
  - /var/log
  - /sandbox/.hermes
  read_write:
  - /sandbox
  - /tmp
  - /dev/null
  - /sandbox/.hermes-data
landlock:
  compatibility: best_effort
process:
  run_as_user: sandbox
  run_as_group: sandbox
network_policies:
  # Optional Tavily search is staged only when TAVILY_API_KEY is configured.
  # The static template grants no Tavily egress. scripts/03-sandbox.sh replaces
  # this marker with one POST /search rule for Hermes's pinned Python runtime.
  # __TAVILY_WEB_SEARCH_POLICY__
  # Load-bearing: per-path allow rules for the NVIDIA inference API.
  # providers/compatible-endpoint.yaml grants read-write over these hosts but
  # cannot express path scoping (v2 ProviderProfile schema has no rules[]).
  # Removing this block widens egress to all /v1/* paths.
  nvidia:
    name: nvidia
    endpoints:
    - host: integrate.api.nvidia.com
      port: 443
      protocol: rest
      tls: terminate
      enforcement: enforce
      rules:
      - allow:
          method: POST
          path: /v1/chat/completions
      - allow:
          method: POST
          path: /v1/completions
      - allow:
          method: POST
          path: /v1/embeddings
      - allow:
          method: GET
          path: /v1/models
      - allow:
          method: GET
          path: /v1/models/**
    - host: inference-api.nvidia.com
      port: 443
      protocol: rest
      tls: terminate
      enforcement: enforce
      rules:
      - allow:
          method: POST
          path: /v1/chat/completions
      - allow:
          method: POST
          path: /v1/completions
      - allow:
          method: POST
          path: /v1/embeddings
      - allow:
          method: GET
          path: /v1/models
      - allow:
          method: GET
          path: /v1/models/**
    binaries:
    - path: /usr/local/bin/hermes
    - path: /opt/hermes/.venv/bin/hermes
    - path: /usr/bin/python3
    - path: /usr/bin/python3.13
    - path: /opt/hermes/.venv/bin/python
  # Credential-less host-routed service — no v2 provider profile equivalent.
  # Sandbox policy is the sole authorization gate.
  phoenix_collector:
    name: phoenix_collector
    endpoints:
    - host: host.openshell.internal
      port: 6006
      protocol: rest
      enforcement: enforce
      allowed_ips:
      - 10.0.0.0/8
      - 172.0.0.0/8
      - 192.168.0.0/16
      - fc00::/7
      rules:
      - allow:
          method: POST
          path: /**
    binaries:
    - path: /usr/bin/python3
    - path: /usr/bin/python3.13
    - path: /opt/hermes/.venv/bin/python
  # Credential-less host-routed service — see phoenix_collector above.
  source_etl_api:
    name: source_etl_api
    endpoints:
    - host: host.openshell.internal
      port: 3100
      protocol: rest
      enforcement: enforce
      allowed_ips:
      - 10.0.0.0/8
      - 172.0.0.0/8
      - 192.168.0.0/16
      - fc00::/7
      rules:
      - allow:
          method: GET
          path: /**
    binaries:
    - path: /usr/bin/python3
    - path: /usr/bin/python3.13
    - path: /opt/hermes/.venv/bin/python
    - path: /usr/local/bin/hermes
    - path: /opt/hermes/.venv/bin/hermes
    - path: /usr/bin/curl
  # Load-bearing: per-repository path scoping for live GitHub REST reads.
  # providers/github.yaml grants read-only over all of api.github.com.
  # This block is the sole gate preventing reads from repositories outside
  # the configured GitHub allowlist. Do not delete without an upstream OpenShell
  # change that adds per-path rules to the v2 ProviderProfile schema.
  github_repo_readonly:
    name: github-repo-readonly
    # scripts/03-sandbox.sh replaces the rule marker below with exact paths
    # for GITHUB_READONLY_REPOS, or the legacy GITHUB_READONLY_REPO fallback.
    endpoints:
    - host: api.github.com
      port: 443
      protocol: rest
      tls: terminate
      enforcement: enforce
      rules:
      - allow: { method: GET, path: /rate_limit }
      # __GITHUB_READONLY_REPOSITORY_RULES__
    binaries:
    - path: /usr/bin/curl
    - path: /usr/bin/python3
    - path: /usr/bin/python3.13
    - path: /opt/hermes/.venv/bin/python
